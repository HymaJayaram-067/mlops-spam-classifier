version: '3.8'

services:
  spam-api:
    build: .
    ports:
      - "5000:5000"
    depends_on:
      - redis
    environment:
      - FLASK_ENV=production

  redis:
    image: redis:7-alpine
    ports:
      - "6379:6379"
